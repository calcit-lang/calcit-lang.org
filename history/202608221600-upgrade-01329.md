# Calcit 0.13.29 upgrade

- Rebases the site upgrade onto current `main`, updates Calcit/procs and dependency metadata to 0.13.29, and keeps `calcit.cirru` as the canonical snapshot.
- Migrates site documentation and CI from the legacy runner/tool names to current `calcit` and `setup-calcit`, and updates the homepage’s displayed Calcit version to 0.13.29.
- Generates analyzer reports before the baseline gate and restricts deployment to pushes on `main`.
- The baseline records one additional partial type position caused by the regenerated nominal schema metadata; other metrics remain unchanged or improve. Local check-only, reports, immutable Yarn install, and JS codegen passed.
